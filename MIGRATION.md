# Инструкция по миграции бота

## Что изменилось

### 1. Баг с временем напоминаний
- **Статус**: Баг отсутствовал в коде
- Напоминания уже отправлялись корректно - за 24 и 3 часа до НАЧАЛА мероприятия

### 2. Упрощение функциональности
- Убраны три кнопки ("Я приду", "Возможно приду", "Не приду")
- Теперь только одна кнопка: "🔔 Напомнить"
- Упрощено сообщение в чате
- Изменен текст подтверждения в личных сообщениях

### 3. Поддержка нескольких чатов
- Теперь при создании события выбирается чат для публикации
- Формат в .env: `COMMUNITY_CHATS=IT клуб:-1001234567890,Дискуссионный клуб:-1009876543210`

### 4. Изменения в базе данных
- Из таблицы `participants` удалено поле `status`
- Теперь участник либо есть в списке (хочет напоминание), либо нет

## Шаги миграции

### Шаг 1: Обновление .env файла

Обновите ваш `.env` файл:

```bash
# Старый формат
COMMUNITY_CHAT_ID=-1001234567890

# Новый формат
COMMUNITY_CHATS=IT клуб:-1001234567890,Дискуссионный клуб:-1009876543210
```

### Шаг 2: Миграция базы данных

Так как изменилась схема базы данных, у вас есть два варианта:

#### Вариант 1: Удалить старую базу (проще, но теряются данные)

```bash
# Остановите бота
# Удалите старую базу данных
rm bot.db

# Запустите бота снова - база создастся автоматически
python main.py
```

#### Вариант 2: Ручная миграция (сохраняет данные)

Если вам нужно сохранить данные, выполните SQL команды:

```sql
-- Создайте резервную копию
.backup bot_backup.db

-- Удалите колонку status из participants
-- SQLite не поддерживает ALTER TABLE DROP COLUMN, поэтому нужно пересоздать таблицу

CREATE TABLE participants_new (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    event_id INTEGER NOT NULL,
    user_id BIGINT NOT NULL,
    username VARCHAR(100),
    fullname VARCHAR(200) NOT NULL,
    timestamp DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (event_id) REFERENCES events(id) ON DELETE CASCADE
);

-- Копируем данные (только тех, кто был в статусе going или maybe)
INSERT INTO participants_new (id, event_id, user_id, username, fullname, timestamp)
SELECT id, event_id, user_id, username, fullname, timestamp
FROM participants
WHERE status IN ('going', 'maybe');

-- Удаляем старую таблицу
DROP TABLE participants;

-- Переименовываем новую таблицу
ALTER TABLE participants_new RENAME TO participants;
```

### Шаг 3: Перезапуск бота

```bash
# Остановите бота (Ctrl+C)
# Запустите бота снова
python main.py
```

## Проверка работы

1. Создайте тестовое событие командой `/event`
2. Выберите чат для публикации
3. Заполните все поля
4. Проверьте, что в чате появилась кнопка "🔔 Напомнить"
5. Нажмите на кнопку и убедитесь, что бот отправил подтверждение в личку
6. Проверьте список участников командой `/list`

## Возможные проблемы

### Ошибка "COMMUNITY_CHATS is not set"
- Убедитесь, что в .env файле правильно указан формат COMMUNITY_CHATS
- Формат: `Название:ID,Название:ID`

### Ошибка при создании события
- Проверьте, что ID чатов правильные
- Убедитесь, что бот добавлен в эти чаты
- Убедитесь, что у бота есть права на отправку сообщений

### Ошибка базы данных
- Если получаете ошибки связанные с колонкой status, удалите базу и создайте заново (вариант 1)
