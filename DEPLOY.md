# 🚀 Инструкция по деплою бота

## Вариант 1: Railway.app (Рекомендуется)

### Шаг 1: Подготовка
1. Зарегистрируйтесь на [Railway.app](https://railway.app/)
2. Подключите свой GitHub аккаунт

### Шаг 2: Загрузка проекта на GitHub
```bash
# Если репозиторий ещё не создан
git init
git add .
git commit -m "Ready for deployment"
git branch -M main
git remote add origin https://github.com/ваш-username/tgbot.git
git push -u origin main
```

### Шаг 3: Деплой на Railway
1. Зайдите в [Railway Dashboard](https://railway.app/dashboard)
2. Нажмите "New Project"
3. Выберите "Deploy from GitHub repo"
4. Выберите ваш репозиторий
5. Railway автоматически определит Python проект

### Шаг 4: Настройка переменных окружения
1. В проекте Railway откройте вкладку "Variables"
2. Добавьте переменные из вашего `.env` файла:
   - `BOT_TOKEN` - ваш токен бота
   - `ADMIN_IDS` - ID администраторов
   - `COMMUNITY_CHAT_ID` - ID чата сообщества

### Шаг 5: Запуск
Railway автоматически запустит бота после настройки переменных.

---

## Вариант 2: Render.com

### Шаг 1: Подготовка
1. Зарегистрируйтесь на [Render.com](https://render.com/)
2. Загрузите проект на GitHub (см. инструкцию выше)

### Шаг 2: Создание сервиса
1. Зайдите в [Render Dashboard](https://dashboard.render.com/)
2. Нажмите "New +" → "Background Worker"
3. Подключите GitHub репозиторий
4. Заполните параметры:
   - **Name**: tgbot-amd
   - **Runtime**: Python 3
   - **Build Command**: `pip install -r requirements.txt`
   - **Start Command**: `python main.py`

### Шаг 3: Настройка переменных
Добавьте переменные окружения в разделе "Environment":
- `BOT_TOKEN`
- `ADMIN_IDS`
- `COMMUNITY_CHAT_ID`

### Шаг 4: Деплой
Нажмите "Create Background Worker" и дождитесь завершения деплоя.

---

## Вариант 3: VPS (Linux сервер)

### Шаг 1: Подключение к серверу
```bash
ssh root@your-server-ip
```

### Шаг 2: Установка зависимостей
```bash
# Обновление системы
apt update && apt upgrade -y

# Установка Python и Git
apt install python3 python3-pip python3-venv git -y
```

### Шаг 3: Клонирование проекта
```bash
cd /opt
git clone https://github.com/ваш-username/tgbot.git
cd tgbot
```

### Шаг 4: Создание виртуального окружения
```bash
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

### Шаг 5: Настройка переменных окружения
```bash
nano .env
# Вставьте ваши переменные:
BOT_TOKEN=ваш_токен
ADMIN_IDS=123456789,987654321
COMMUNITY_CHAT_ID=-1001234567890
```

### Шаг 6: Создание systemd сервиса
```bash
nano /etc/systemd/system/tgbot.service
```

Вставьте следующее содержимое:
```ini
[Unit]
Description=Telegram Bot AMD
After=network.target

[Service]
Type=simple
User=root
WorkingDirectory=/opt/tgbot
Environment="PATH=/opt/tgbot/venv/bin"
ExecStart=/opt/tgbot/venv/bin/python /opt/tgbot/main.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

### Шаг 7: Запуск сервиса
```bash
systemctl daemon-reload
systemctl enable tgbot
systemctl start tgbot
systemctl status tgbot
```

### Просмотр логов:
```bash
journalctl -u tgbot -f
```

---

## 📊 Сравнение вариантов

| Платформа | Цена | Сложность | Стабильность | Рекомендация |
|-----------|------|-----------|--------------|--------------|
| Railway.app | Бесплатно (500ч) | ⭐ Легко | ⭐⭐⭐ Отлично | Для старта |
| Render.com | Бесплатно | ⭐⭐ Средне | ⭐⭐ Хорошо | Альтернатива |
| VPS | От 300₽/мес | ⭐⭐⭐ Сложно | ⭐⭐⭐ Отлично | Для продакшена |

---

## 🔧 Устранение проблем

### Бот не запускается
- Проверьте переменные окружения
- Проверьте логи: `journalctl -u tgbot -f` (VPS) или в веб-интерфейсе

### База данных не сохраняется
- На Railway/Render используйте PostgreSQL вместо SQLite
- Добавьте volume для постоянного хранения

### Бот засыпает
- На Render бесплатный план засыпает - переключитесь на платный или VPS

---

## ✅ Чеклист деплоя

- [ ] Проект загружен на GitHub
- [ ] Создан файл `.env` с переменными
- [ ] `.env` добавлен в `.gitignore`
- [ ] Установлены все зависимости из `requirements.txt`
- [ ] Бот протестирован локально
- [ ] Выбрана платформа для хостинга
- [ ] Настроены переменные окружения на платформе
- [ ] Бот запущен и работает
- [ ] Проверена работа напоминаний

---

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте логи бота
2. Убедитесь, что все переменные окружения настроены
3. Проверьте, что токен бота действителен
4. Убедитесь, что у бота есть права в чате
