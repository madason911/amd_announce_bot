# Telegram-бот для анонсов мероприятий сообщества

Автоматизированный бот для публикации анонсов мероприятий, сбора откликов участников и отправки напоминаний.

## Возможности

- Создание анонсов мероприятий через диалоговый интерфейс
- Сбор откликов участников через интерактивные кнопки
- Автоматические напоминания за 24 часа и 3 часа до события
- Просмотр списка участников для администраторов
- Очистка старых мероприятий
- Поддержка 1000+ пользователей

## Требования

- Python 3.10+
- Telegram Bot Token (получить у @BotFather)
- ID администраторов
- ID чата сообщества

## Установка

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd tgbot
```

### 2. Создание виртуального окружения

```bash
python -m venv venv
source venv/bin/activate  # для Linux/macOS
# или
venv\Scripts\activate  # для Windows
```

### 3. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 4. Настройка конфигурации

Скопируйте `.env.example` в `.env`:

```bash
cp .env.example .env
```

Откройте `.env` и заполните необходимые переменные:

```env
BOT_TOKEN=your_bot_token_here
ADMIN_IDS=123456789,987654321
COMMUNITY_CHAT_ID=-1001234567890
DATABASE_URL=sqlite+aiosqlite:///./bot.db
```

#### Как получить необходимые данные:

1. **BOT_TOKEN**: Создайте бота через @BotFather и скопируйте токен
2. **ADMIN_IDS**: Отправьте любое сообщение боту @userinfobot, он вернет ваш ID
3. **COMMUNITY_CHAT_ID**:
   - Добавьте @userinfobot в ваш чат
   - Перешлите любое сообщение из чата в @userinfobot
   - Скопируйте ID чата (будет начинаться с `-100`)

### 5. Настройка бота

Добавьте бота в ваш чат сообщества и выдайте ему права администратора (для отправки сообщений).

## Запуск

```bash
python main.py
```

## Использование

### Для администраторов

#### Создание мероприятия

1. Отправьте команду `/event`
2. Следуйте инструкциям бота:
   - Введите название мероприятия
   - Введите дату и время (формат: `DD.MM HH:MM`, например `15.12 18:00`)
   - Введите адрес
   - Введите описание

Бот автоматически опубликует анонс в чате сообщества с кнопками для откликов.

#### Просмотр участников

Отправьте команду `/list` для просмотра списка участников предстоящих мероприятий.

#### Очистка старых мероприятий

Отправьте команду `/clear_events` для удаления прошедших мероприятий из базы данных.

### Для участников

1. Увидев анонс в чате, нажмите одну из кнопок:
   - 👍 Я приду
   - 🤔 Возможно приду
   - 👎 Не приду

2. Если вы выбрали "Я приду" или "Возможно приду", вы получите напоминания:
   - За 24 часа до события
   - За 3 часа до события

## Структура проекта

```
tgbot/
├── bot/
│   ├── handlers/
│   │   ├── event.py          # Обработчики создания мероприятий
│   │   ├── callbacks.py      # Обработчики откликов
│   │   └── admin.py          # Административные команды
│   ├── states.py             # FSM состояния
│   └── keyboards.py          # Клавиатуры
├── database/
│   ├── models.py             # Модели базы данных
│   └── __init__.py
├── scheduler.py              # Планировщик напоминаний
├── config.py                 # Конфигурация
├── main.py                   # Точка входа
├── requirements.txt          # Зависимости
├── .env.example              # Пример конфигурации
├── .gitignore
└── README.md
```

## База данных

Бот использует SQLite для хранения данных. База создается автоматически при первом запуске.

### Таблицы:

- `events` - мероприятия
- `participants` - участники и их отклики

## Деплой

### Railway.app

1. Создайте аккаунт на [Railway.app](https://railway.app)
2. Подключите репозиторий
3. Добавьте переменные окружения
4. Railway автоматически запустит бота

### Render.com

1. Создайте аккаунт на [Render.com](https://render.com)
2. Создайте новый Web Service
3. Подключите репозиторий
4. Установите команду запуска: `python main.py`
5. Добавьте переменные окружения

### VPS

1. Подключитесь к серверу
2. Клонируйте репозиторий
3. Установите зависимости
4. Настройте systemd service для автозапуска:

```bash
sudo nano /etc/systemd/system/tgbot.service
```

```ini
[Unit]
Description=Telegram Event Bot
After=network.target

[Service]
Type=simple
User=your_user
WorkingDirectory=/path/to/tgbot
Environment="PATH=/path/to/tgbot/venv/bin"
ExecStart=/path/to/tgbot/venv/bin/python main.py
Restart=always

[Install]
WantedBy=multi-user.target
```

```bash
sudo systemctl enable tgbot
sudo systemctl start tgbot
```

## Логирование

Бот ведет логи всех операций. Уровень логирования: INFO.

Логи включают:
- Создание мероприятий
- Отправку напоминаний
- Ошибки при отправке сообщений

## Устранение неполадок

### Бот не отправляет напоминания

1. Проверьте, что пользователь взаимодействовал с ботом (нажал кнопку)
2. Убедитесь, что планировщик запущен (проверьте логи)

### Бот не публикует в чат

1. Убедитесь, что бот добавлен в чат
2. Проверьте, что у бота есть права на отправку сообщений
3. Проверьте правильность COMMUNITY_CHAT_ID

### Ошибка "BOT_TOKEN is not set"

Убедитесь, что файл `.env` создан и содержит корректные данные.

## Лицензия

MIT

## Поддержка

Для вопросов и предложений создайте issue в репозитории.
